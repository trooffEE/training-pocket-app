name: Go-test
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Setup Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.25.1'
      - name: Build API
        run: go build -o api ./cmd/api/main.go -v
      - name: Build Telegram
        run: go build -o telegram ./cmd/telegram/main.go -v
      - name: Test with the Go CLI
        run: go test ./...
